import CodeBlock from "@theme/CodeBlock";
import ExampleBlock from "../examples/ExampleBlock";
import Example from "../examples/UseNextFrame";
import ExampleCode from "!!raw-loader!../examples/UseNextFrame";

Ensure the callback is invoked after the current paiting.

## API

```ts
function useNextFrame(
  callback: () => void
): [start: () => void, cancel: () => void];
```

Params:

- **`callback`:** the function that will be invoked.

Results:

- **`start`:** schedule function.
- **`cancel`:** cancel function.

## Example

<ExampleBlock>
  <Example />
</ExampleBlock>
<CodeBlock language="tsx">{ExampleCode}</CodeBlock>
