import CodeBlock from "@theme/CodeBlock";
import ExampleBlock from "../examples/ExampleBlock";
import Example from "../examples/UseToggle";
import ExampleCode from "!!raw-loader!../examples/UseToggle";

Use boolean value.

## API

```ts
function useToggle(initialValue?: boolean): [
  value: boolean,
  actions: {
    toggle: (newValue?: boolean) => void;
    toggleOn: () => void;
    toggleOff: () => void;
  }
];
```

Params:

- **`initialValue`:** optional initial value, default is false.

Results:

- **`value`:** a boolean value, can be changed by the following functions.
- **`toggle`:** toggle the value. If the newValue param is specified, the value will be set to the newValue.
- **`toggleOn`:** set the value to true.
- **`toggleOff`:** set the value to false.

## Example

<ExampleBlock>
  <Example />
</ExampleBlock>
<CodeBlock language="tsx">{ExampleCode}</CodeBlock>
