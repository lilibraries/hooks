import CodeBlock from "@theme/CodeBlock";
import ExampleBlock from "../examples/ExampleBlock";
import Example from "../examples/UseTitle";
import ExampleCode from "!!raw-loader!../examples/UseTitle";

Set document title.

## API

```ts
function useTitle(title: string, options?: { restore?: boolean }): void;
```

Params:

- **`title`:** document title.
- **`options.restore`:** restore previous title on the component unmount.

## Example

<ExampleBlock>
  <Example />
</ExampleBlock>
<CodeBlock language="tsx">{ExampleCode}</CodeBlock>
