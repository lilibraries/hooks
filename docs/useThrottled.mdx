import CodeBlock from "@theme/CodeBlock";
import ExampleBlock from "../examples/ExampleBlock";
import Example from "../examples/UseThrottled";
import ExampleCode from "!!raw-loader!../examples/UseThrottled";

Throttle a value.

## API

```ts
function useThrottled<T = any>(
  value: T,
  options?: number | { wait?: number; leading?: boolean; trailing?: boolean }
): [
  throttled: T,
  actions: {
    flush: (finalValue?: T) => void;
    cancel: () => void;
  }
];
```

Params:

- **`value`:** The value that will be changed.
- **`options`:** Throttle wait time or an options object.
  - **`options.wait`:** Throttle wait time, in milliseconds. Default is `0`.
  - **`options.leading`:** Change throttled value on the leading edge. Default is `false`.
  - **`options.trailing`:** Change throttled value on the trailing edge. Default is `true`.

Results:

- **`throttled`:** Throttled value.
- **`actions.flush`:** Flush funciton. The `finalValue` will be returned as `throttled` if it is specified.
- **`actions.cancel`:** Cancel throttle timer.

## Example

<ExampleBlock>
  <Example />
</ExampleBlock>
<CodeBlock language="tsx">{ExampleCode}</CodeBlock>
