import CodeBlock from "@theme/CodeBlock";
import ExampleBlock from "../examples/ExampleBlock";
import Example from "../examples/UseTimeout";
import ExampleCode from "!!raw-loader!../examples/UseTimeout";

Delay running callback.

## API

```ts
function useTimeout(
  callback: () => void,
  delay?: number
): [start: () => void, cancel: () => void];
```

### Params

- **`callback`:** The function that will be delayed calling.
- **`delay`:** Optional delay time, in milliseconds.

### Results

- **`start`:** Set a timer. Automatically clear the previous timer.
- **`cancel`:** Cancel timer.

## Example

<ExampleBlock>
  <Example />
</ExampleBlock>
<CodeBlock language="tsx">{ExampleCode}</CodeBlock>
